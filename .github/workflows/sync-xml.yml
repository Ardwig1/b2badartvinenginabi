name: Daily XML Sync

on:
  schedule:
    # Her gece 23:15 TR (20:15 UTC)
    - cron: '15 20 * * *'
  workflow_dispatch:
    # Manuel tetikleme

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger XML Sync API
        run: |
          curl -X GET "https://enginb2b.vercel.app/api/cron/sync-xml" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          -H "x-vercel-cron: 1"
